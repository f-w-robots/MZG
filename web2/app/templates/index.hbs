{{outlet}}
<div id="left-column">
  <b>Available devices</b>, {{#link-to 'devices.new'}}new{{/link-to}}
  <br>
  {{#each model.devices as |device|}}
    <b>{{device.hwid}}</b> -
    {{#if device.manual}}
      manual,
    {{else}}
      algorithm,
    {{/if}}
    {{#link-to 'devices.edit' device}}edit{{/link-to}},
    <a href="#" {{action "deleteDevice" device}}>delete</a>
    <br>
  {{/each}}
  <br>
  <b>Interfaces</b>, {{#link-to 'interface.new'}}new{{/link-to}}
  <br>
  {{interfaces}}
  {{#each model.interfaces as |interface|}}
    <b>{{interface.interfaceId}}</b>
    {{#link-to 'interface.edit' interface}}edit{{/link-to}},
    <a href="#" {{action "deleteInterface" interface}}>delete</a>
    <br>
  {{/each}}
</div>
<div id="right-column">
  <b>Manual control</b>
  <br>
  <div class="connectBlock">
    <select multiple="multiple" size="3" id="selectList">
    </select>
    <br>
    <button id="selectButton">Select</button>
    <button disabled>Update</button>
  </div>
  <div class="controlBlock">

  </div>
</div>
