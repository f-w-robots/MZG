<div class="col-md-4">
  {{#if success}}
    <div class="alert alert-success" role="alert">
      Saved!
    </div>
  {{/if}}
  {{#if error}}
    <div class="alert alert-danger" role="alert">
      "Error"
    </div>
  {{/if}}
  <!--<form>
    <div class="row">
      <div class="form-group">
        Username: {{input value=username class="form-control"}}
      </div>
      <div class="form-group">
        Password: {{input value=password class="form-control"}}
      </div>
    </div>
    <div class="btn btn-success" {{action "signup"}}>SignUP</div>
  </form> -->
  <div class="contactImputBlock row">
    <div class="imputLeftBlock">
    Username: {{input value=username}}
    Password: {{input value=password}}
    </div>
    <div class="btn btn-success" {{action "signup"}}>SignUP</div>
  </div>
</div>
