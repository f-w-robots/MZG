{
  "data": [
  <% @record.each_with_index do |model, index| %>
    {
      "type": "devices",
      "id": "<%= model[:hwid] %>",
      "attributes": {
        "manual": <%= model[:manual] %>,
        "hwid": "<%= model[:hwid] %>"
      }
    }<%= @records.count - 1 == index ? '' : ',' %>
  <% end %>
  ]
}
